spring:
  application:
    name: gulimall-ware
  cloud:
    nacos:
      config:
        server-addr: localhost:8848   # Nacos 本地地址
        file-extension: yaml          # 配置文件后缀
#        namespace: public             # 默认 public
      discovery:
        username: nacos
        password: nacos
        server-addr: localhost:8848   # 如果用注册中心，也配上
